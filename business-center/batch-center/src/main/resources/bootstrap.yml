server:
#  port: 5000    #固定端口
   port: 5009 #随机端口
spring:
  application:
    name: batch-center

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always        
      
